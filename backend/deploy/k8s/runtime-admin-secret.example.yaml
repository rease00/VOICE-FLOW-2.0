apiVersion: v1
kind: Secret
metadata:
  name: voiceflow-runtime-admin
  namespace: voiceflow
type: Opaque
stringData:
  # Shared admin token used by media-backend and gemini-runtime admin endpoints.
  GEMINI_RUNTIME_ADMIN_TOKEN: "replace_with_strong_runtime_admin_token"