/**
 * Firebase / Firestore stub
 * Replace with real Firebase config when ready.
 */

import type { Firestore } from 'firebase/firestore';

// Firestore instance — null until Firebase is configured
export const db: Firestore | null = null;
