export const BILLING_CHECKOUT_LOCK_TITLE = 'Billing checkout is temporarily paused';
export const BILLING_CHECKOUT_LOCK_MESSAGE =
  'Checkout is temporarily paused while V FLOW AI completes launch checks. Existing users can still access the studio, but billing changes stay locked until launch.';

export const isBillingCheckoutLocked = (): boolean => true;
