import { chromium } from "playwright";(async () => { const browser = await chromium.launch({ headless: true }); const page = await browser.newPage(); await page.goto("http://localhost:3000/app/library"); await page.waitForTimeout(2000); const html = await page.content(); require("fs").writeFileSync("page.html", html); await browser.close();})();