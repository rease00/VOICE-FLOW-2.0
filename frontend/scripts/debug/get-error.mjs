import { chromium } from "playwright";(async () => { const browser = await chromium.launch({ headless: true }); const page = await browser.newPage(); await page.goto("http://localhost:3000/app/library"); await page.waitForTimeout(1000); await page.getByRole("button", { name: "AI Chat", exact: true }).click(); await page.waitForTimeout(2000); console.log(await page.locator("body").innerText()); await browser.close();})();